# **Enumerations**
Each file must declare which domain it is part of by starting with a Level 1 heading with the domain name. 

### **Enum Declaration**

Enums appear under:

```markdown
## Enums
```

Each enum uses a level‑3 heading:

```markdown
### Loyalty Tier
```

### **Enum Definition**

````markdown
```yaml
values:
  - Bronze
  - Silver
  - Gold
  - Platinum
```
````

---